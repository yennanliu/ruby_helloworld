<h1>Products</h1>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= product.name %>,
       <%= product.id %>,
       <%= product.created_at %>
    </div>
  <% end %>
</div>
